FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ src/
COPY configs/ configs/
RUN pip install -e .

EXPOSE 8080

ENTRYPOINT ["specinferkit"]
CMD ["serve", "--checkpoint", "/models/default", "--port", "8080"]
